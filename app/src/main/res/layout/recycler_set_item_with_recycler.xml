<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="ContentDescription">

    <data>

        <import type="android.view.View" />

        <variable
            name="title"
            type="String" />

        <variable
            name="hideMenu"
            type="Boolean" />

        <variable
            name="listener"
            type="android.view.View.OnClickListener" />

        <variable
            name="menuListener"
            type="android.view.View.OnClickListener" />
    </data>

    <android.support.v7.widget.CardView
        style="@style/CardViewClickable"
        android:onClickListener="@{listener}">

        <RelativeLayout style="@style/WrapContentWrapContent">

            <TextView
                style="@style/TextHeader.Dark"
                android:text="@{title}" />

            <ImageView
                style="@style/ImageMore.Dark"
                android:onClickListener="@{menuListener}"
                android:visibility="@{hideMenu?View.GONE:View.VISIBLE, default = visible}" />

            <android.support.v7.widget.RecyclerView
                android:id="@+id/recycler"
                style="@style/RecyclerChildVertical"
                android:layout_marginBottom="12dp"
                android:layout_marginTop="48dp" />
        </RelativeLayout>
    </android.support.v7.widget.CardView>
</layout>